document.getElementById("simulacion-form").addEventListener("submit", function (event) {
    event.preventDefault();

    // Abre una nueva página después de iniciar la simulación
    window.open("mostrarTabla.html", "_blank");
});
